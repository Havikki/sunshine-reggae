- name: Asenna vsftpd
  apt:
    name: vsftpd
    state: present

- name: Kopioi vsftpd.conf
  copy:
    src: vsftpd.conf
    dest: /etc/vsftpd.conf
    owner: root
    group: root
    mode: '0644'
  notify: Käynnistä vsftpd uudelleen
